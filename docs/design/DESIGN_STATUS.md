# Design Status

Tracker for the design-first gate (see `HANDOFF.md` §6–§7). A spec may only be
coded after it is marked **verified** here.

| # | Spec | Written | Verified | Reviewer | Notes |
|---|---|---|---|---|---|
| 00 | `00-architecture.md` | yes | **verified** | Oracle pass 2: GATE PASS (16 findings fixed; no open HIGH/MEDIUM) | §5 items 1–3 closed; discovery + live-testing folded in; §8.1 + §9.2 errata |
| 01 | `01-session.md` | yes | **verified** | Oracle component gate: GATE PASS (7 findings fixed; no open HIGH/MEDIUM) | 1544 lines; F1–F12 + S1–S14; `turn/fail` errata (additive); decisions in §16 |
| 02 | `02-persistence.md` | yes | **verified** | Oracle component gate: GATE PASS (7 findings fixed; no open HIGH/MEDIUM) | 1529 lines; P1–P18 + P-F1–P-F16; read-only open mode; same-DB snapshot errata |
| 03 | `03-workspace-registry.md` | yes | **verified** | Oracle component gate: GATE PASS (5 findings fixed; no open HIGH/MEDIUM) | 1340 lines; R1–R16 + R-F1–R-F18; DDL byte-identical to §9.10 |
| 04 | `04-workspace-host-daemon.md` | yes | **verified** | Oracle component gate: GATE PASS (6 findings fixed; no open HIGH/MEDIUM) | 1473 lines; H1–H21 + D-F1–D-F22; focus-independent activation |
| 05 | `05-transport.md` | yes | **verified** | Oracle component gate: GATE PASS (13 findings fixed; no open HIGH/MEDIUM) | 1704 lines; T1–T24 + T-F1–T-F22; cursor resume, `id:null`, profiles |
| 06 | `06-agent-loop.md` | yes | **verified** | Oracle component gate: GATE PASS (16 findings fixed; no open HIGH/MEDIUM) | 1424 lines; A1–A18 + A-F1–A-F18; `LLMPool` pinned; AgentRegistry owns lifetime |
| 07 | `07-tools-execution.md` | yes | **verified** | Oracle component gate: GATE PASS (16 findings fixed; no open HIGH/MEDIUM) | 1743 lines; X1–X16 + E-F1–E-F18; symlink-safe `resolve()`; `ToolConfig` |
| 08 | `08-llm-provider.md` | yes | **verified** | Oracle component gate: GATE PASS (8 findings fixed; no open HIGH/MEDIUM) | 1390 lines; L1–L17 + L-F1–L-F18; retry barrier, `turn/fail`; §4.5 errata delegates `TurnFailed.code` to 06 |
| 09 | `09-permissions.md` | yes | **verified** | Oracle component gate: GATE PASS (12 findings fixed; no open HIGH/MEDIUM) | 1360 lines; Q1–Q15 + Q-F1–Q-F16; `PermissionConfig`; F2 surface-via-attention |
| 10 | `10-supervisor-tui.md` | yes | **verified** | Oracle component gate: GATE PASS (8 findings fixed; no open HIGH/MEDIUM) | 1626 lines; U1–U18 + U-F1–U-F20; focus-independent flash |
| 11 | `11-m2-errata.md` | yes | **verified** | Oracle G0 interface-freeze gate: PASS (21 findings fixed; no open HIGH/MEDIUM) | 1426 lines; M2 interfaces frozen; amends 03/04/05/09/10; post-gate additive §7.2 `ClockReader` amendment (track E) + E21 |
| 12 | `12-m1-drift-errata.md` | yes | — (register; not a component spec) | track F (self), pending review | 204 lines; M1 spec-text ↔ code reconciliation register DR1–DR6; defers to 11 (no new semantics) |
| 13 | `13-context-compaction.md` | yes | **verified** | Oracle component gate: GATE PASS (8 review rounds; no open HIGH/MEDIUM) | 1567+ lines; context compaction (projection, never deletion); `ContextCompaction` event |
| 14 | `14-pty-capability.md` | yes | **verified** | Oracle component gate: GATE PASS (4 review rounds; no open HIGH/MEDIUM) | 1865+ lines; `PtySession`/`PtyService` behind the execution-environment seam |
| 15 | `15-mcp-adapter.md` | yes | **verified** | Oracle component gate: GATE PASS (3 review rounds; no open HIGH/MEDIUM) | 2051 lines; MCP client adapter into the shared tool registry; namespaced `mcp.<server>.<tool>` |
| 16 | `16-daemon-ownership.md` | yes | **verified + implemented** | Dual gate, 5 rounds: adversarial critic + Oracle, **both PASS** (0 open HIGH/MEDIUM) | 2737 lines; supervisor-owned daemons; supersedes 04 H8/H9 §3.2/§6.5/§14.1(f), 00 §54 D23/§9.8/§9.9, 10 §2.1, 11 §8.2 D20.3; amends 03/04/05 (A15/A16); invariants O1–O22, failure modes O-F1–O-F16. **Implemented in 8 waves** (`036e4e4e1`…`065e221d9`); verified live with two supervisors against real DeepSeek |
| — | `REQUIREMENTS_BACKLOG.md` | yes | — (register; not a component spec) | track: triage, self-verified | 468 lines; RB-01–RB-15 from `requirements_draft.txt`, dedup'd against shipped code; RB-13 (switcher/sessions) done per spec 22, RB-14/RB-15 recorded in spec 22 §11 |
| — | `UI_SURFACE_INVENTORY.md` | yes | — (register; not a component spec) | track: triage, self-verified | 163 lines; spec-16 ↔ RB-10/RB-11 UI seam (C1–C5), Surface 3 updated for spec 22 (live-only switcher + `/sessions` History source) |
| 17 | `17-ui-transcript-errata.md` | yes | **verified** | Oracle gate: 3 rounds → **GATE PASS** (0 open HIGH/MEDIUM/LOW; 18 findings fixed) | 577 lines; additive errata to `10-supervisor-tui.md` for RB-01/02/08/10/11; invariants `U-RB01-1..3`, `U-RB02-1..4`, `U-RB08-1..4`, `U-RB10-*`, `U-RB11-*` |
| 18 | `18-context-errata.md` | yes | **verified + implemented** | Oracle gate: 7 rounds → **PASS** (0 open HIGH/MEDIUM; 7 LOW) | additive errata to `10` (UI) + `13` (compaction) for RB-06 `/context`; color grid + MCP/tools inventory. Gate forced: `context_generation_` made atomic (was a UI/pump-thread data race), a generation guard so a late reply cannot re-open a dismissed overlay, and `last_error` never shipped (only a boolean `has_error`). Rounds 4–6 were all one defect class — the fixed chrome budget — resolved by one global decision (CX-D12). **Implemented in `edd696c6e`** |
| 19 | `19-session-rename-errata.md` | yes | **verified + implemented** | Oracle gate: 4 rounds → **PASS** (0 open HIGH/MEDIUM; 1 LOW) | 1566 lines; additive errata to `01` (session/event log) + `05` (transport) for RB-03 (auto-name + rename); fixed an io-thread/worker-thread race in the auto-name path and made manual-rename-wins structural (two independent guards). **Implemented in `162fa8641`**; verified live against real DeepSeek |
| 20 | `20-skills.md` | yes | **verified + implemented** | Oracle gate: 3 rounds → **PASS** (0 open HIGH/MEDIUM; 10 LOW) | 2273 lines; NEW skills subsystem for RB-05 (discovery, `/skills`, invocation, context injection, trust tiers); gate caught a construction-order bug (the skill index never reached the model) and a trust-boundary hole (a relative user root acquiring user trust). **Implemented in `6761e62ef`**; verified live |
| 21 | `21-config-jsonc-errata.md` | yes | **verified + implemented** | Oracle gate: 5 rounds → **PASS** (0 open HIGH/MEDIUM; 7 LOW); Rev 5–8 amendment re-gate → **PASS** (0 open HIGH/MEDIUM) | additive errata to `08` config spec for RB-09; **JSONC only, TOML retired** (user decision 2026-09-16); parser claim verified against nlohmann/json 3.12.0 (comments yes, trailing commas no). Rev 5–8 adds **21-D11**–**21-D17**: no TOML awareness, global config required, explicit `--config` never scaffolded, the top-level unknown-key message, supervisor→daemon `--config` handoff, a present path must be a regular file, and the config-load gate (`Tui`/`Run`/`List`/`Show`/`Replay`/`Fork` only); **`toml++` retired** (not fetched). **Implemented** (`9db17cd54`; Rev 5–8 applied in the working tree) |
| 22 | `22-switcher-sessions-errata.md` | yes | **verified + implemented** | Oracle gate: 5 rounds → **PASS** (0 open HIGH/MEDIUM) | Rev 5; 1865 lines; additive errata to `10`/`16`/`03` for S1–S4: live-only Ctrl-S switcher + daemon-death eviction (supersedes "ALL workspaces" in `00` §20.24 / `10` §7.1 and the `NotRunning` browsability in `16` §3.2.1/§7.7), a new `/sessions` disk catalog (extend, not duplicate; O14/C4), spawn/attach-then-resume on selection, and `ymh --resume <id>` in TUI mode. Invariants SW1–SW26, failure modes SW-F1–SW-F19. **Implemented** |
| 23 | `23-session-lifecycle-errata.md` | yes | **verified** | Oracle (adversarial gate): PASS — zero open HIGH/MEDIUM | **Rev 13 — verified (adversarial Oracle gate, PASS — zero open HIGH/MEDIUM; 4 LOW wording/completeness fixed; no creation-path/wire/protocol/TUI-state-machine change); scope: the user's frozen-creation-path requirement — cleanup-on-exit of unprompted sessions + `ymh session prune --empty` + the subagent-kind pre-ship fix + legacy-empty filters + the guard-order fix + the flock-scoped orphan sweep**; **Rev 12 — Rev 11 gate-fail fix (1 MEDIUM, 0 HIGH, 2 LOW; the Rev 11 production design confirmed correct and reachable end-to-end — the deletion-gap design, the `pending_creates_` snapshot, and the production empty-create path; the MEDIUM was the missing active-workspace harness seam (`focus_workspace` pinned, Rev 12), the LOWs the §12.1 harness-inventory wording and the §3.8 `track()` preservation)**; **Rev 11 — Rev 10 gate-fail fix (1 MEDIUM, 0 HIGH; the Rev 10 design fix confirmed sound — no interleaving leaves an accepted prompt's id in `created_sessions_` with all three records empty; the rest of Rev 10 verified clean)**; **Rev 10 — Rev 9 gate-fail fix (1 MEDIUM, 0 HIGH; the rest of Rev 9 verified clean)**; **Rev 9 — Rev 8 gate-fail fix (3 MEDIUM, 0 HIGH; the rest of Rev 8 verified clean); Rev 8 and Rev 7 rescope retained**; RB-19; D1′ **unprompted-session cleanup on exit**: at a clean supervisor exit, before spec-16 deregister/teardown, the supervisor deletes each unprompted root session **it created** via `session.delete{only_if_empty:true, force:true}` (best-effort; `ymh session prune --empty` is the backstop). **The creation path is frozen**: `session.create` and the TUI eager creates (`src/ui/supervisor.cpp:1215`,`:1425`,`:1905`; dead `:388`) are retained; no wire retirement, no protocol bump, no TUI no-session state. **The Rev 6 `agent.prompt`-creates pivot is WITHDRAWN** (non-convergent under a 5-lens hyperplan + five Oracle rounds: self-deadlock, unimplementable drain, cycle leak, missing `EventBus` publish that killed the live stream, unmarshalled turn); the creation-gate work is NOT pursued. D2 `ymh session prune` kept whole but scoped to `--empty` only (`--older-than` deferred); hybrid (candidates from disk via `openReadOnly`; live deletes via `session.delete`; stopped via manager + `WorkspaceRegistry::removeSession`), dry-run by default, junction-first ordering with a **dependent precheck before any side effect**, orphan-junction sweep **only in flock-held contexts with a fresh-store guard**, root-only, PTY close, `--keep 0` guard. D3 **subagent-kind fix is a PRE-SHIP blocker** (`SessionOptions.kind/parentSession`; `SubagentRunner` passes Subagent/parent). D4 legacy empties **hidden, not auto-deleted**; filter at the catalog consumer (`include_unprompted` defaults true) so `ymh --resume <hidden-id>` resolves. D5 sweep fresh-store guard. Amends `01` §9.1 create timing (restored), `05` (additive `session.delete.only_if_empty`/`force` only), `02`, `03` R16, `16` §4.3 (prepend cleanup step 0), `06`/subagent kind, `10`/`22` (filter), `19` (auto-name unchanged). **Rev 8** fixes the three Rev 7 gate MEDIUMs with no creation-path/wire/protocol change: **MEDIUM-1** the supervisor records a per-process `prompted_sessions_` set at prompt-submission time and cleanup targets `created_sessions_ \ prompted_sessions_` (a queued prompt with no durable `user/message` is never deleted); **MEDIUM-2** `refresh_sessions` gates auto-create on the **unfiltered registry junction set** (`WorkspaceRegistry::listSessions`), never the D4-filtered `session.list` reply, so a workspace whose only sessions are unprompted roots no longer gets a spurious empty and `--resume <hidden-id>` is preserved; **MEDIUM-3** the false `activate_session`→`HostRuntime::active_session_` chain is corrected (`active_session_` is set only by the `session.activate` RPC, which no production client sends), so `force` is repurposed as defence-in-depth and SL-U7 explicitly sends `session.activate`. Also **L1** Ctrl-S cell reconciliation in `refresh_sessions` (absent cells removed, active exempt), **L2** subagent junction count corrected to 0, and the created-set pinned as a **distinct per-process** record (`SupervisorConnection::track` is a union, not the source; prior-run/crash empties fall to `prune`). Invariants SL1–SL28, failure modes SL-F1–SL-F26, tests SL-U1–SL-U14 / SL-I1–SL-I28 / SL-P1–SL-P5. RB-21 (pre-existing lifetime/teardown hazards) remains deferred and NOT load-bearing. **Rev 9** fixes the three Rev 8 gate MEDIUMs with no creation-path/wire/protocol change: **MEDIUM-1** the whole reply-driven update of `refresh_sessions` (cell add, Ctrl-S reconcile and `UiModel::eraseSession`, and the auto-create decision) is gated on `reply.ok && reply.result.is_array()`, so a failed/timed-out `session.list` reply removes no cell and no `SessionUiState`/input draft and creates no session; **MEDIUM-2** `created_sessions_` and `prompted_sessions_` share one `cleanup_mutex_`, and the `session.create` reply handler binds the returned id to the already-captured queued prompt and records both sets in the same critical section before `apply_create_reply`, so no accepted prompt can be overtaken and the container data race is gone; **MEDIUM-3** the auto-create gate is the **union** of the registry junction set and the reply, with the daemon's D4 skip scoped to `entry.inJunction` so store-only (pre-M2/crash-window) entries surface — no wire change. Also four LOWs (cross-supervisor residual softened, §6.5 citation corrected, §13.2#14 store-only attribution corrected, §2.1 heading) and the SL-I13 typo. Invariants SL1–SL28 (Rev 9 amends SL4/SL14/SL27/SL28), failure modes SL-F1–SL-F29, tests SL-U1–SL-U15 / SL-I1–SL-I30 / SL-P1–SL-P5. RB-21 remains deferred and NOT load-bearing. **Rev 10** fixes the one remaining Rev 9 gate MEDIUM (MEDIUM-2 ordering: a prompt queued after the `session.create` reply but before `apply_create_reply` is drained) by snapshotting `pending_creates_` in the same `cleanup_mutex_` critical section and skipping every created id whose workspace has a non-empty pending entry; the §3.3 invariant is corrected to that actual condition, SL14 is qualified with the active-session exemption (LOW-1), and the reply-execution thread citation is corrected to `SupervisorConnection::process_requests` (`src/ui/supervisor_connection.cpp:362`; LOW-2). Tests add SL-U16. Invariants SL1–SL28 (Rev 10 amends SL4/SL14/SL27), failure modes SL-F1–SL-F29 (Rev 10 amends SL-F28), tests SL-U1–SL-U16 / SL-I1–SL-I30 / SL-P1–SL-P5. No creation-path/wire/protocol change. **Rev 11** fixes the one remaining Rev 10 gate MEDIUM (MEDIUM-1: SL-U16's first driver called `submit()` with empty text, which returns at `src/ui/supervisor.cpp:368-370` before its create call at `:388`, so the test created no session and passed without the `pending_creates_` snapshot — vacuous; the driver is now a real production empty-create via `handle_input`'s first keystroke `:1905`, `new_session` `:1425`, or the harness `create_session(ws,"")` seam), plus three LOWs: **LOW-2** `cleanup_mutex_` is pinned **leaf-only** (container operations only; never held across `submit`, the reply-cv wait, or `connection->stop()`); **LOW-3** the SL-U15/SL-U16 harness seams are pinned in §12.1 (`submit`, `create_session`, `run_create_reply`/extracted `on_create_reply`, `confirm_exit`, `take_delete_requests`; `dispatch_key` is the first-keystroke driver); **LOW-4** §3.1 reads the `pending_creates_` **snapshot**, not the live map. No creation-path/wire/protocol/TUI-state-machine change. |
| 24 | `24-agent-lifetime-errata.md` | yes | **verified** | Independent adversarial gate: 5 rounds → **GATE PASS** (0 open HIGH/MEDIUM; 3 LOW, documentation-only). **NOTE: Oracle (the project's named gate) was unavailable — two attempts returned empty output — so verification is by an independent adversarial reviewer under `AGENTS.md`'s "Oracle / review team" allowance** | 2374 lines; repairs the four RB-21 session/agent lifetime & teardown defects plus the user's detach decision. **Structural, not enumerated**: the raw accessors `AgentRegistry::find`/`get` and `SessionManager::AgentLookup`/`setAgentLookup`/`findAgent`/`agent` are **deleted** and `session()` becomes private, so a raw map-derived agent/session handle is unobtainable **by construction**; every caller holds `findShared`/`getShared`/`sessionPtr`, copied under `AgentRegistry::mutex_` and used outside it. Repo-wide enforcement is a concrete CTest (`tests/unit/no_raw_map_handle_test.cpp`, `AL-U22`). Gate history: R1 1H/9M/5L → R2 1H/3M/2L → R3 1H/2M/1L → R4 1H/2L → R5 0H/2M/2L → R6 **PASS**. Invariants AL1–AL37, failure modes AL-F1–AL-F29, decisions 24-D1–24-D19. **User decisions**: detach is real — a non-last supervisor's exit leaves the turn running (daemon-owned, controllable from a running supervisor); the last supervisor's exit saves-to-history and closes (`finalizeAll`→`closeSession`); hard exit on grace expiry; no new wire notification. Amends 01/02/04/05/06/11/16/22/23 |

| 25 | `25-ui-ux-errata.md` | yes | **verified at Rev 5; Rev 6–8 are POST-GATE amendments pending re-gate** | Oracle gate: 4 rounds (v1 FAIL 2 HIGH/5 MEDIUM → Rev 5 **GATE PASS**, 0 HIGH/0 MEDIUM, 6 LOW); the later revisions were NOT re-gated | 2190 lines; the eight requested UI/config items (status line replacing "idle"; palette off-by-one + Enter-accepts-highlight; `/exit` without a dialog; first-run localcode import; `/skill` removed; `/quit` alias; localcode `mcp_servers` shape) plus dsh's plan mode (durable `plan/mode` event + a projection folding the log, a `plan:policy` guidance section, the always-registered `exit_plan_mode` tool reviewed through the existing Ask flow, `/plan` + `/plan off`; **guidance-only — no tool denial**). The gates caught a permanent-startup-brick in the import and a review dialog that would have shown no plan. **Implemented in `dfadfa6d2`**; 1147/1147 + 7/7 live |
| 26 | `26-dsh-alignment.md` (+ `-part2`) | yes | **verified (Rev 7)** | **GATE PASS — 0 HIGH / 0 MEDIUM / 5 LOW.** Round history: 13H/27M/16L → 1H/6M/5L → 0H/5M/5L → Rev 6 (M-3: the event vocabulary is decoupled from `kProtocolVersion`; the wire receiver skips unknown event types while the on-disk decode keeps its loud forward-fence) → Rev 7 (M-1's mechanism: a DEDICATED `session.persist_prompt_text` opt-in, default OFF, distinct from the spdlog `log_prompts`; the **global-layer-only** rule holds by design, so a workspace config cannot enable prompt capture). Five of the six MEDIUMs were first found by an adversarial planning panel | the dsh design-copy program the user requested: a faithful mechanics study, a gap analysis vs ymh, a pinned C++23 target design, and a 6-wave migration plan. Explicitly **design** alignment, not code copying |

| 27 | `27-session-locking-errata.md` | yes | **verified (Rev 4)** | Independent gates: Rev 1 FAIL (2 MEDIUM/6 LOW) → Rev 2 FAIL (0H/1M/7L) → Rev 3 GATE PASS (0H/0M/5L) → **Rev 4 GATE PASS (0 HIGH / 0 MEDIUM / 4 LOW)** after closing the 27-R1 `header()` latent hazard | 639 lines; the F3 locking contract for the already-shipped fix (27-I1..I5: the accessors lock `appendMutex_` for the whole read and return copies; the load-bearing re-entrancy invariant covering both inline-publish paths and `subscribe()`-fired handlers, with EACH trigger class naming the mutex it self-deadlocks on — plain-append → `appendMutex_`, `set()`/`commit_`/`erase` re-entry → `commit_mutex_`; the tabulated lock order incl. `commit_mutex_ → SessionManager::mutex_`; `reload()` construction-only; `erase()` a noexcept leaf; 27-I2 audit-only with a pinned verification artifact §7.1). Reconciles spec 01's I7/I18/I21 and supersedes spec 25 §N12; closes spec 25 §14.1's F3 row. Records the `header()` latent API hazard as follow-up 27-R1. **This legitimises the uncommitted F3 fix** — it is no longer code-first drift |

| 29 | `29-event-family-errata.md` | yes | **verified** | Independent gate: **GATE PASS — 0 HIGH / 0 MEDIUM / 8 LOW** (citation-range drift only). The cross-check re-grepped the 70-label consumer count EXACT (session.cpp 22 no-default + ui_event_adapter 25 + session_export 9 + session_cli 8 + headless 6 = 70 across 7 switches, in TUs only; the repo-wide 80 includes docs). Verified: the 6-point codec checklist is complete; the two-axis rule is faithful; the wire-skip gap is genuinely unimplemented (a poison-pill reconnect, not understated); the `AssistantChunk` live-only change is safe; the headless regression is real. The reviewer did not manufacture a MEDIUM | 488 lines; the Wave-0 A1 event-family freeze (29-D1..D8) |
| 30 | `30-architecture-cascade-errata.md` | yes | **verified — independent re-gate PASS (0H/0M); P1/P2/P3 recorded 2026-09-19; the TOP-LEVEL GATE PASSES** | Round 1: GATE FAIL (1 HIGH + 1 MEDIUM + LOWs). **Both fixed:** the HIGH was a real sequencing defect (Stage A claimed to be complete at 00/01/08, but the `06` agent-loop errata is a Wave-1 prerequisite — Wave 1 changes `AgentLoop::buildRequest` and re-seams `ContextCompactor`, both 06-owned; independently confirmed by spec 28 §13.1). The MEDIUM was a §4.2-vs-§4.4 classification contradiction on spec `08`, now reconciled to the register per §4.4's own "register wins" rule and recorded as a known spec-26 defect | 447 lines; the Wave-0 A0 top-level-gate amendment (amends `00` BY REFERENCE): the four seams S1–S4, the cascade table, the Stage A/B restaging, the three program prerequisites |

| 28 | `28-llm-service-boundary-errata.md` | yes | **verified (Rev 3)** | Independent gates: Rev 0/1 FAIL (0H/2M/4L) → Rev 2 GATE PASS (0H/0M/1L) → **Rev 3 GATE PASS (0 HIGH / 0 MEDIUM / 3 LOW)**. Rev 3 pins the cross-cutting provider-id source (28-D9, §3.5): `LlmCallConfig::provider` ← a new `AgentConfig::provider` (sourced from `config.llm.provider`, no second key) → the runtime's registered default route when unset → a loud typed `LLMErrorCode::NoProviderRoute` failure at `prepare_call`; the first-dispatch case (`held_config` nullopt) is pinned in §5.3; the required spec-21 amendment is recorded. Specs 31 and 32 reference this pin and define no competing source | 1047 lines |

| 31 | `31-agent-loop-errata.md` | yes | **verified (Rev 2)** | Independent gates: Rev 1 FAIL (0 HIGH / 1 MEDIUM / 6 LOW) → **Rev 2 GATE PASS (0 HIGH / 0 MEDIUM / 3 LOW)**. The MEDIUM (the provider source) is fixed via spec 28 Rev 3's pin (A23, A-F23, 31-D7) with no competing source; the 6 LOWs fixed (agent_loop.hpp now includes the runtime header; the LlmRequestHeader scan via Session::events(); the L18–L25 range; the RAII LLMPool::Slot; the enumerated test callers; A-F23). The reviewer re-grepped the tree and confirmed the caller list is COMPLETE | 777 lines |
| 32 | `32-compaction-errata.md` | yes | **verified (Rev 1)** | Independent gates: Rev 0 FAIL (2 HIGH / 2 MEDIUM / 3 LOW) → **Rev 1 GATE PASS (0 HIGH / 0 MEDIUM / 2 LOW)**. Rev 1 fixed: H1 (the caller enumeration — the reviewer independently re-grepped and confirmed exactly 17 construction sites across 3 files: the 15 `compaction_test.cpp` lines + the daemon + the fixture); H2 (the ledger — spec 13 truly runs A1–A11, so the additions renumber to A12/A13); M1 (the pruner pins replace-by-id, chosen because spec 26 pins ContextPrune as projection-ignore, with the token accounting now exact); M2 (the provider source references spec 28 Rev 3's pin via the 13-owned `CompactionPolicy::provider` — "one source, not two") | 562 lines |

| 33 | `33-stream-event-codec-errata.md` | yes | **verified** | Independent gate: **GATE PASS (0 HIGH / 0 MEDIUM / 7 LOW)**. Pins the MISSING `StreamEvent` JSON codec (GAP 1 of the Wave-2 design-first audit — the specs referred to an "existing StreamEvent codec" that did not exist). Verified against the shipped `include/ymh/llm/stream.hpp`: all EIGHT alternatives confirmed exact, every payload field correct, the discriminators total/unique/snake_case with zero collisions, the omission rules pinned, and the round-trip identity (33-I1) in the test plan. Both dangling references discharged by-reference | 554 lines |

| 34 | `34-assembler-replay-errata.md` | yes | **verified (Rev 4)** — re-gate PASS (0 HIGH / 0 MEDIUM; 2 LOW): Rev 4 restores live token-by-token streaming, the retry marker, the no-double-print rule, the retained bound, and the reasoning rule (Rev 3 was verified PASS 0 HIGH / 0 MEDIUM / 6 LOW). Rev 4 restores live token-by-token streaming in the shared `AssistantStreamPrinter` (34-D13, 34-I11–I16, 34-F9, §15 item 1 R1–R5): `Text` deltas stream to `out` as they arrive; the `AssistantMessage` settlement prints only the not-yet-streamed durable suffix (durable is authority); a non-`Completed` `AssistantAttempt` emits `kRetryMarker` on `err`; the retained record stays `kMaxBufferedBytes`-bounded | Independent gates: Rev 1 FAIL (0 HIGH / 1 MEDIUM / 6 LOW) → **Rev 2 VERIFIED (0 HIGH / 0 MEDIUM / 0 LOW)**, verified by a reviewer who did not write it. Closes GAP 2 + GAP 3: the block-assembly algorithm (reproduces the shipped agent_loop.cpp:~927-936), `TimedStreamEvent.at` (a steady_clock delta from the per-attempt stream start), the per-attempt sink/assembler/accumulator wiring, `interrupted_blocks()`, and the per-attempt SETTLEMENT PREDICATE (Completed → AssistantMessage; Cancelled/Failed → AssistantAttempt — replacing the unconditional append at ~:938-944), plus the test-only replay harness with a typed `ReplayMismatch` surface. The Rev-1 MEDIUM was subtle: the replay prefix bound was `seq <= header_seq`, which reconstructs as of the SERIES START (a header is logged only on template change) and the harness PASSED on that wrong prefix; Rev 2 bounds it POSITIONALLY (`prefix == log[0..k)`, `log[k].seq == settlement_seq`) — independently confirmed, including that `settlement_seq - 1` appears only as a prohibition (the DB-wide AUTOINCREMENT means a session's sequences are NOT contiguous). **Requires re-gates of 26 §4.3.9.1 (the ReplayEnvelope row) and 30 §5.3 (Stage-B row 2)** | 1045 lines |

| 35 | `35-live-notification-errata.md` | yes | **gated FAIL 0H/1M/3L — MEDIUM-1 fixed, re-gate pending** | not yet re-gated | 708 lines; pins the `event.live` JSON-RPC notification that the Wave-2 H1 fix added unpinned (a design-first gate violation the re-check caught): the wire name, the `LiveNotification{envelope}` payload, the per-(connection,session) audience, the bus-publish ordering, the **no-cursor/no-replay** rule, and NO `kProtocolVersion` bump. Decision 35-D1 keeps a DISTINCT notification (rejecting `event.stream` routing — it is `SubscriptionId`-keyed and needs a post-event cursor a live-only event cannot have). Also records the required spec-05 edits, applied here: §5.1's "only live signals" sentence (which had also OMITTED the pre-existing `event.unsubscribed`) and §7.7's notification catalog |

| 36 | `36-prompt-registry.md` | yes | **verified (Rev 1)** — independent gate: Rev 1 FAIL (1 HIGH / 7 MEDIUM / 5 LOW) → fixed → **re-gate PASS (0 HIGH / 0 MEDIUM; 5 LOW)** | not yet committed at gate time | 1233 lines; the Wave-3 owning spec: the ordered section registry + `assemble()`/`render_prompt()`, persona (harness identity + a persona), the AGENTS.md/CLAUDE.md loader, runtime-context snapshots, tool presentation Native, the `MessageSource`/`ContextForm` provenance reshaping the `01` message model, the `default_system_prompt()` migration, and the digest/replay interaction. Keeps ymh's identity (per `26-part2:1591-1595`); `session.persist_prompt_text` stays default `false` (recorded P2) |
| 37 | `37-message-provenance-errata.md` | yes | **verified (Rev 2)** — re-gate PASS (0 HIGH / 0 MEDIUM; 2 new LOW) (Rev 1 gate FAIL 1 HIGH / 3 MEDIUM / 1 LOW) | not yet re-gated | 774 lines; the `01` errata that `36 §3.2`/§3.3 and `36-D7`/`36-D8` require: the `Message`/payload provenance fields, the codecs, the `ContextInjected.role` default, the `deriveMessages()` projection, and the snapshot-equivalence rule. Rev 2 fixes the gate: pins the producer sites + value derivation (`agent_loop.cpp` `:373-386,472-492,584-719,963-971`, §5.4/37-D13), the structural default member initializers and makes the canonical codec default the structural default (§5/§5.1, 37-D11), the durable nested JSON schema + enum strings (§5.3, 37-D12), and reconciles the snapshot-invalidation claim with the test-only sequence-only durable path (§7.3, OQ-7). Invariants `37-I1`-`37-I12`, failure modes `37-F1`-`37-F12`, decisions `37-D1`-`37-D14` |
| 38 | `38-transcript-provenance-errata.md` | yes | **verified (Rev 2)** — re-gate PASS (0 HIGH / 0 MEDIUM; 2 new LOW) (Rev 1 gate FAIL 3 HIGH / 1 MEDIUM / 2 LOW) | not yet re-gated | 627 lines; the `17` errata that `36 §3.3` requires: the `Context` row kind and `ContextFormed`/`MessageSource` fields, the `ContextInjected`/`SystemPromptShown` UI events, the form-label rendering, the tool notice suffix, and the preserved `18` chrome/generation/`has_error` contracts. Rev 2 fixes the gate: `ConversationEntry.context`/`ToolCallView.notice` are `ContextFormed` (§3/38-D2); the tool-notice data path is pinned via `ui::ToolFinished.context` -> `apply` -> `ToolCallView.notice` (§4.3/38-D10); existing UI events gain `source`/`context` (§4.1/38-D5); `SystemPromptShown` multiplicity is one updated row (§4.2/38-D11); the contradiction with verified `26 §4.3.9.2:986` is recorded as OQ-2, not silently resolved (38-D12). Invariants `38-I1`-`38-I11`, failure modes `38-F1`-`38-F11`, decisions `38-D1`-`38-D12` |

| 39 | `39-session-persist-prompt-text-errata.md` | yes | **verified (Rev 1) — re-gate PASS (0 HIGH / 0 MEDIUM)** | gate FAIL (1 MEDIUM / 3 LOW) → fixed → re-gate PASS | 408 lines; amends `21` by reference and owns `session.persist_prompt_text` (the `[session]` section, `bool`, default `false`, global-layer-only — a workspace occurrence is a `ConfigError` — governing the session-DB full-prompt copy only, distinct from `logging.log_prompts`). Closes the ONE gap the mechanical spec-symbol catalog test found (the recorded Wave-1 design-first debt) |

| 40 | `40-output-retention.md` | yes | **verified (Rev 1) — re-gate PASS (0 HIGH / 0 MEDIUM)** | gate FAIL (1 MEDIUM / 3 LOW) → fixed → re-gate PASS | 984 lines; the Wave-4 owning spec: the bounded-parallel scheduler (D10), the retention library + the `ToolResult` omission metadata (D11), the tool-result pruner (D12), the compaction trigger taxonomy (D13), the repeat-tool reminders, and retiring `clamp_tool_result` (D21). References the verified spec 32; honors the Wave-3→4 serialization (40-I16) |
| 41 | `41-retry-executor.md` | yes | **verified (Rev 1) — re-gate PASS (0 HIGH / 0 MEDIUM)** | gate FAIL (1 MEDIUM / 4 LOW) → fixed → re-gate PASS | 1016 lines; owns the separate durable retry executor (26-I3): the placement at a durable step boundary, the policy/backoff, the `llm/retry`/`llm/retry_started` events, the settlement interaction (spec 34), the `LlmRuntime` boundary (spec 28), and the no-double-call guarantees. **Does NOT activate it** — honors `26 OQ4`; activation timing stays an open user question |

| 42 | `42-agent-presets.md` | yes | **verified (Rev 1) — re-gate PASS (0 HIGH / 0 MEDIUM)** | gate FAIL (2 HIGH / 5 MEDIUM / 4 LOW) → fixed → re-gate PASS | 1127 lines; the Wave-5 owning spec (agent presets + subagent composition): the roster, the standing mount + joined scopes, the blank-session-only switch, the `agent_preset`/`agent_preset_selected` event, `apply_child_composition`, the depth limit, the fixed delegation scope, and the session header preset id (the `23` break). **Residual MEDIUM (pre-existing, recorded):** the fixed-delegation-scope approval-`never` has no assigned seam |
| 43 | `43-wave5-dependency-errata.md` | yes | **verified (Rev 1) — re-gate PASS (0 HIGH / 0 MEDIUM)** | authored during the spec-42 fix pass → re-gate PASS | The Wave-5 dependency errata: amends `23`/`01`/`21` by reference + the additive `06` child-creation obligation (the creation-path copy, the header codec, the `presets.*` keys) |

| 44 | `44-goals-jobs-commands.md` | yes | **verified (Rev 2) — re-gate PASS (0 HIGH / 0 MEDIUM)** | gate FAIL (2 MEDIUM / 3 LOW) → fixed → re-gate PASS | 1309 lines; the Wave-6 owning spec (the final wave): the durable goal state + the round driver + `/goal` (D18), the owner-scoped job registry + `job_output`/`job_list`/`job_kill` + the wakeup policy (D19), and the log-only `command`/`run|done` + the agent-scoped shadowing (D20). Supersedes the reserved `30-goals-jobs-commands.md` |

| 45 | `45-ui-interaction-errata.md` | yes | **verified (Rev 6) — re-gate PASS (0 HIGH / 0 MEDIUM; 1 LOW)** | gate FAIL (2 HIGH/10 MEDIUM/8 LOW) → fixed → FAIL (1 MEDIUM) → fixed → critic PASS → **Oracle DO NOT APPROVE (4 HIGH/6 MEDIUM)** → fixed → FAIL (3 MEDIUM/3 LOW) → fixed → FAIL (1 MEDIUM/2 LOW) → fixed → **PASS** | 1990 lines; the UI interaction errata pinning 10 behaviors: input history over prompts+commands (D1), the `/` command list navigated by arrows with Tab completing the selected command and Esc dismissing it (D2/D5), the Ctrl+S switcher as a strict subset of `/sessions` with the focused session hidden from both (D3/D4; truthful `(current session hidden)` / `(no stored sessions)` / `(loading live sessions…)` placeholders, no node suppression), the new `mcp.status` / `agent.list` / `agent.select` RPCs (`kProtocolVersion` stays 1; `MethodNotFound` degradation for all three; shared JSON **schema** via two serializers, not a shared function), `/status` (D7; `connected` derived from `state == "ready"`), `/quit` rendered as `exit(quit)` (D8), Tab/Shift+Tab agent cycling gated to an empty non-`/` draft (D9), and the unmodeled-session lockout fix (D10; `activate_session` models the session; `activeSessionId` private with a single mutator; every keystroke is no longer dropped). Supersedes `17 §5` RB-08 Tab-cycling and `25-D8`/`25-D12` by reference. **Residual LOW (recorded):** the "why not a shared builder" rationale is inaccurate — `ymh_agent` does transitively reach `ymh_mcp` via `ymh::config`; the two-serializer design stands, only the justification needs rewording | 1572 lines; the UI interaction errata pinning 10 behaviors: input history over prompts+commands (D1), the `/` command list navigated by arrows with Tab completing the selected command and Esc dismissing it (D2/D5), the Ctrl+S switcher as a strict subset of `/sessions` with the focused session hidden from both (D3/D4, with the distinct `(current session hidden)` vs `(no stored sessions)` cases), the new `mcp.status` / `agent.list` / `agent.select` RPCs with `kProtocolVersion` staying 1 (D6/D9), `/status` (D7), `/quit` rendered as `exit(quit)` (D8), Tab/Shift+Tab agent cycling gated on `/` being inactive (D9), and the unmodeled-session lockout fix (`activate_session` models the session; every keystroke is no longer dropped) (D10). Supersedes `17 §5` RB-08 Tab-cycling and `25-D8`/`25-D12` by reference |
| 46 | `46-permissions-ui-errata.md` | yes | **verified** | Oracle final re-review (rev 6): **APPROVE — 0 HIGH / 0 MEDIUM / 3 LOW** (the 3 LOWs were documentation-only and are fixed). Gate history: gate1 FAIL 9H/32M/15L; gate2 FAIL 2H/19M/18L; gate3 FAIL 0H/4M/5L; Oracle rev4 DO NOT APPROVE 3H/7M/3L; Oracle rev5 DO NOT APPROVE 1H/1M/4L | Rev 6 (rev 5 was 4316, rev 4 3772, rev 3 3464, rev 2 3045, rev 1 2110). Rev 6 is the final convergence round: it responds to the Oracle re-review (`/tmp/opencode/spec46-oracle-rereview.md`, DO NOT APPROVE 1H/1M/4L), which confirmed 12 of 13 prior findings closed and the approved areas regression-free, and fixes only the new findings — the `resume_in_flight_` release moves out of `recover_unknown_session` (two non-resume callers would underflow it) into the resume-failure terminal via a guarded `release_resume` (NEW-1), an explicit config **deny** now vetoes a durable `Always` grant (NEW-2), I1 distinguishes config-rule from grants-file `path` keys (NEW-3), the daemon order guards the `findByCanonicalPath` optional (NEW-4), the `kRedacted` citation is corrected to `:10` (NEW-5), and I36 is restated to the shape-based form (NEW-6). The tool-wide durable path grant is recorded as an accepted limitation. Rev 5 (rev 4 was 3772, rev 3 3464, rev 2 3045, rev 1 2110). Responds to all four reports (§23.1 gate-1 F-01…F-56; §23.2 gate-2 G2-H1/H2, G2-M1…G2-M19, G2-L1…G2-L18; §23.3 gate-3 N1…N4 + N5…N9; §23.4 Oracle O-H1…O-H3, O-M1…O-M7, O-L1…O-L3). Rev 5 fixes the three Oracle HIGHs: D2 durable grants and D1 config rules are rescoped to **tool + command only** (the `path` dimension is dropped — the shipped loop never populates `PermissionRequest::path`, so a path grant silently degraded to tool-wide and a path rule was dead) and grants are matched **literally** (O-H1/O-M1); the `GrantStore` construction/ownership/injection seam and the daemon startup order are pinned (`WorkspaceRuntimeOptions::grant_store`; registry open + id resolution before `WorkspaceRuntime::create`) (O-H2); the `resume_in_flight_` marker becomes a per-workspace **refcount** backstopped by a `SupervisorConnection` disconnect drain that replies to every pending request (O-H3/O-M6). Also fixes the seven MEDIUMs (confinement claim for `shell`/`git`/`mcp.*`, `max_tokens` route, `PolicyConfigError`→`ConfigError`, `force_ask` guard in `remember()`, log-seam redaction, escaping/literal matching) and the three LOWs. All 13 items covered. Invariants `46-I1`–`46-I37`, failure modes `46-F1`–`46-F40` |
| 47 | `47-muse-glimmer-support.md` | yes | **verified** | Delta re-gate (rev 4): **GATE PASS — 0 HIGH / 0 MEDIUM / 1 LOW**; gate1 FAIL 3H/13M/5L; Oracle rev2 DO NOT APPROVE 2H/5M/3L; Oracle rev3 DO NOT APPROVE 0H/2M/3L. | rev 4; Muse-Glimmer-30B support (D1–D11): the model-profile seam (`llm.default.profile`), the forced first tool call, the `<|eom|>` stop guard, leaked-call detection, the native-ATEM detection grammar, argument normalization, the sampling/tool-choice config surface, `top_k` end-to-end, per-provider capabilities, `xhigh`, and the reversibility contract. Rev 2 responds to `/tmp/opencode/spec47-gate1.md` (all 21 findings H1–H3/M1–M13/L1–L5 resolved or rebutted in §23): the reversibility invariant is restated as confined Muse identity with a grep test; the ATEM history/echo-back path is pinned in both argument shapes; the leaked-call posture is whole-content-only; D2 keys on the turn-local step; citations re-verified. Rev 1 corrected plan finding A's token id (`<|eom|>`=200007, `<|eot|>`=200008, D3 keys on the string) and plan D7's current-state claim. Rev 3 responds to `/tmp/opencode/spec47-oracle.md` (all 10 findings 47-O-H1…47-O-L3 resolved in §23.4) and **descopes D5 to detection-only**: the `"muse-glimmer-atem"` profile, the ATEM declaration renderer, the history echo-back, and the ATEM→`ToolCall` converter are removed (the server Jinja template owns history rendering; an untranslated endpoint is a new spec); native ATEM in content is detected and surfaced as a typed error, never converted or executed. 47-O-H1 moves D6 normalization to the assembler's finalize (after fragment concatenation); 47-O-H2 (Rev 4, 47-O2-M1) closes the `required`-with-no-`tools` 400 structurally — the body builder is unchanged and no profile field can omit `tools[]`, so the Rev-3 `wire_tools_present` conjunct and `MuseD2_WireToolsGuard` are removed; 47-O2-M2 widens `parse_leaked_json_call` to a `LeakedParse` (`complete_block_seen`/`malformed_block_seen`) so the whole G-JSON taxonomy is decidable; 47-O-M3 pins the decoder's `ToolCallPolicy` inputs; 47-O-M4 pins `buildRequest`'s missing turn-local `turn_step`; 47-O-M5 moves D4/D5/D6 end-to-end tests onto the real adapter + fake transport; 47-O-L1/L2/L3 pin the scaffold placeholder, `top_k:0`→`nullopt`, and the D4 taxonomy. The token ruling is retained unchanged |
| 48 | `48-ui-and-config-errata.md` | yes | **draft (Rev 7)** — not yet reviewed | pending independent gate | Rev 7 (**split by user decision**); TUI/input errata (D2/D4/D5/D6/D7/D8): the Esc-Esc interrupt with the exact hint `- one more <Esc> to interrupt` (D2), `Ctrl+←/→` word motion (D4), a real glyph-safe composer caret at `InputModel::cursor` (D5), the user-bright / intermediate-dim / final-normal styling policy (D6), one-line rich tool lines with span-aware, content-box-width truncation (D7), and a blank line after each reasoning block (D8). 48-I1–48-I20, 48-F1–48-F14. **Rev 7: the original nine-item 48 was split by risk; the higher-risk half (skill/command discovery, the fresh-launch session guarantee, the MCP child environment, and the MCP global-layer-only guard) moved to spec 50. 48-D10 (MCP 401/403 hints) was DROPPED by user decision (the underlying problem was an expired credential, not a harness defect); its four gate findings are gone. The `notice:`/collapsed-line contradiction is reconciled (48-I15/OQ-48-8).** Verification status: DRAFT — not yet reviewed |
| 49 | `49-switcher-single-workspace.md` | yes | **draft (Rev 3)** — not yet reviewed | pending independent gate | Rev 3 (gate FAIL: 1 HIGH + 7 MEDIUM → repaired; **not verified**). Rev 3 fixes: the lazy path models/attaches only **after** `ensureRunning` (`ensure_worker_loop`), never pre-spawn; the first draft rides `pending_creates_` consumed on `on_link_state(Attached)` (no `pending_prompts_`); `find_or_register_workspace` replaced by a supervisor-local registry helper (`options_.registry->findByCanonicalPath`/`registerWorkspace`); 16-D2's bare-cwd `NotRunning` listing clause amended; the fabricated "48-D3.3 cwd always wins" reference removed (real overlap 46-D7/46-I13/23-D58/50-D2); the active-workspace unknown-membership fallback added (49-D5.1/49-I13); Ctrl+N/`/new` corrected as a non-trigger in the zero-workspace state. Rev 2; **lazy workspace/daemon creation on the first prompt (primary)** plus the Ctrl-S "nothing to switch to" notice (secondary). **New user decision (2026-09-21):** a bare `ymh` creates no workspace and no daemon, shows an empty screen, and creates the workspace only on the first prompt `<Enter>`; ordering pinned submit → register cwd row → queue draft → spawn → attach-on-register → create session → send prompt. Root cause of the Rev-1 report was the eager cwd spawn (`src/cli/cli.cpp:514`, authorized by 22 §11.4/22-A7); **spec 16 §3.2.1 (16-D2) already pins lazy spawn**, so the shipped code is the drift — **22 §11.4/22-A7 bare-cwd eager spawn is superseded and 16-D2's policy reinstated (49-A1/49-A11)**. **Spec 16 verdict: a zero-daemon supervisor is already representable** — no FK in the `supervisors` DDL (`src/registry/registry.cpp:91-103`; `16:250-252`), registration is workspace-independent (`src/ui/supervisor.cpp:426,836-849`), and O1/O10/O15 are vacuous with an empty `connections_` (`compute_orphaning_set`/`confirm_exit`/`teardown_daemons`); no schema/interface change. Shipped blockers found: `run()` returns 1 on empty `workspaces` (`supervisor.cpp:406-408`) and `submit()` never spawns (`:437-440`). Decisions 49-D1–D10; amendment register 49-A1–A12 (46-D3.2/3.3/3.5/I8 amended; 22 §3.1/§3.4/§3.7 + 45-D3/D4 NOT amended; 16-D2 listing clause amended); invariants 49-I1–I14; failure modes 49-F1–49-F11; tests 49-U1–U15, 49-G1–G5, 49-P1–P4; 7 open questions (OQ-49-7 resolved and recorded). Verification status: DRAFT (Rev 3) — not yet reviewed |
| 50 | `50-skills-mcp-and-session-lifecycle-errata.md` | yes | **implemented (Rev 3)** — errata recorded; pending gate re-review | implemented; gate re-review pending | Rev 3 (implementation errata: **50-D5 workspace trust gate** — the workspace tier is fail-closed absent an outside trust record; `~/.ymh` scaffolding not shipped; the file-command contract narrowed to a discovery library). Rev 2 (gate FAIL: 1 HIGH + MEDIUMs → repaired; **not verified**). Rev 2 fixes: **50-D2.3 reconciled with spec 49's lazy spawn** (cwd-wins scoped to when the workspace exists; no spec appended from a nonexistent row; no spurious notice on a bare launch; 50-I8 restated, 50-F6 scoped to explicit activation); **50-I13/50-D3.5 stderr routing corrected** — raw MCP child stderr goes to a dedicated per-server `<workspace>/.ymh/mcp/<server>.stderr.log` (0600), not `host.log`/`config_.log_sink` (which is ymh's own `O_TRUNC` log, so the Rev-1 "outside 50-I13" carve-out was false); **50-D1.4 reserved mechanism made real** (new `Command::reserved` field; the struct had none); MCP global-layer guard re-verified (`apply_mcp`/`apply_mcp_servers_object` take no `global_layer` → no `-Wunused-parameter`); **cross-spec reconciliation §13 added** and the fabricated "48-D3.3 cwd always wins" reference removed (48 has no `D3`). Rev 1 (**split from spec 48 by user decision**); Skills/MCP & session-lifecycle errata (D1–D4): Claude-Code skill/command discovery from `~/.ymh` + `$HOME/.claude` with scaffolding + precedence (D1), the fresh-launch session guarantee + cwd-workspace startup fix for the Ctrl+C re-entry bug (D2, reconciled with spec 49's lazy spawn), the MCP child environment fix (`clearenv()` → inherit-and-overlay) as an independent defect with `env_mode`/`Minimal` scoped to the keys ymh seeds and child-injected `LC_CTYPE` outside (D3), and the MCP global-layer-only guard (D4, breaking) that closes a workspace-config supply-chain vector. Item 9's jira 401 is RESOLVED (invalid expired/revoked credential, not a harness defect); the credential-validation procedure is preserved. 50-I1–50-I24, 50-F1–50-F18. Verification status: implemented (Rev 3) — pending gate re-review |

| 51 | `51-reasoning-fold-prompt-emphasis-and-tables-errata.md` | yes | **verified + implemented (Rev 3)** | Implemented in `f474e35eb` (D3 tables), `745eefdde` (D2 emphasis), `5a90971a3` (D4 Ctrl+Q/Ctrl+D); hermetic suite 1944/1944. Rev 2 gate **DO NOT APPROVE** (2 HIGH / 10 MEDIUM / 8 LOW, from an Oracle gate + an adversarial critic) → Rev 3 fixes → independent re-check **PASS (0 HIGH / 0 MEDIUM)** | Rev 3: independent-review fixes (5 HIGH, 10 MEDIUM, 8 LOW). **H1** — session delete is a `session.delete` JSON-RPC to the **owning daemon** (the supervisor opens sessions.db read-only and has no `SessionManager`; §6.1 cites both CLI paths `apply_live`/`apply_stopped`); **H2** — the RPC passes `only_if_empty=false, force=false` so the daemon's active-session refusal is preserved (the Rev-2 `deleteSession(id, /*force=*/true)` mislabel is dropped); **H3** — the composer left bar changes the `ConversationSnapshot` composer row `│>`→`││ > ` + 66 spaces + `│` (72 cols, pinned); **H4** — the `user_bar` recolour requires editing `UserBrightAndIntermediateDimmedInTranscript` to `\x1b[38;2;92;156;245m` and supersedes 48-D6.5; **H5** — table layout uses `RenderContext::indent` (block quote/list/user-gutter) so nested tables are not clipped; plus M1–M10 (delete-arm binding/reset, new refusal modes F19–F22, post-delete cursor clamp, Live-workspace-delete-always-refused, stale Ctrl+D hints, exact `wrap_cell` row height, span-safe alignment, corrected coverage, I1 scoping) and L1–L12 (I19 conditional, 45-I25/16 supersession, count source, Ctrl+Q raw mode, `make_theme` inline site, file-local interfaces, duplicate-test removal, fallback hard-break). Rev 2: **D1 (inline reasoning folding) WITHDRAWN by user decision** — the premise was never observed (no Muse assistant message in any log; a tree-wide grep for `<think` finds nothing) and the correct `reasoning_content` channel already folds (47 set `capabilities.reasoning = true`, `src/llm/model_profile.cpp:27`), so there is nothing to build; inline tags in `content`, if any, are the model's defect. **D2 (user-prompt emphasis)** corrected: the dead `true_color` gate is deleted (`Theme` has no such field; `src/ui/supervisor.cpp:3041` aggregate-initializes `color`), the minimal change is pinned (`user_block_background` appended after the shipped fields, `user_bar` recoloured to `RGB(92,156,245)`), a field-order rule protects the 2-field golden aggregate inits (`tests/unit/ui_render_golden_test.cpp:841`), the composer-left-bar decision is pinned (the composer DOES get it; the caret goldens `CaretCursorLandsAtInputPosition` `:2029` / `CaretCursorHandlesCjkLeadingCell` `:2044` shift by 2 columns), and D2 tests move from unlinkable file-local `paint_bg` to render-level ANSI goldens. **D3 (markdown tables)** corrected (six blocking errors): `align_left`/`align_center` do not exist in FTXUI 6.1.9 (use `align_right`/`hcenter`/`paragraphAlign*`), dispatch uses `cmark_node_get_type_string` (no `table.h`/`CMARK_NODE_TABLE*` symbols) with the `"table_header"` vs shared numeric type `32781` header trap pinned, variable row heights are modeled, the "never truncate" invariant is corrected with a hard-break rule, the fallback synthesizes pipe text from cell spans and is kept, and the shrink loop is retained. **D4 added:** exit moves to Ctrl+Q (`begin_exit(true)`, still raising the 16 §7.6 confirmation); Ctrl+D in the Ctrl+S / `/sessions` overlay (one shared `SwitcherOverlayModel`, `ui_model.hpp:403`) deletes the highlighted session (when `cursor.session` is set) or workspace behind a reused 48-D2 double-press arm; the **session delete is a daemon `session.delete` RPC** (`only_if_empty=false`, `force=false`; the daemon performs both layers and refuses the active/dependent session), the session-count workspace cascade (count from `WorkspaceRegistry::listSessions`), the live-workspace refusal ("stop the workspace first"; a Live-source workspace delete is **always** refused; stop-then-delete is OQ-51-6; a session delete in a non-running workspace is refused, OQ-51-9), and the registry-only on-disk boundary (no `.ymh/` removal; OQ-51-7) are pinned; specs 45 §1.1/D8/**45-I25**, 22 §3.6/§4.3 and 16 §7.6/**16's Ctrl+D refs** are amended/superseded. Invariants 51-I1–I27, failure modes 51-F1–F22. Amends specs 10, 22 and 45 (no longer 47; the theme-token clause refines 17/48-D6.1 by reference and **supersedes 48-D6.5**). Anchors pinned to HEAD `e9a644a30` (suite 1895/1895 green). Verification status: DRAFT (Rev 3) — not yet reviewed |
| 52 | `52-endpoints-models-and-dsh-agent-presets.md` | yes | **verified (Rev 2)** | Five-reviewer adversarial gate (scope, schema, import, Part B, completeness): Rev 1 → **7 HIGH / 14 MEDIUM / 18 LOW** → Rev 2 → re-check **all findings FIXED**, 0 open HIGH/MEDIUM (2 LOW residuals recorded). Note: the full-file gate failed repeatedly on a tooling limit (the review agents could not read a 1200-line file); it was run as five section-scoped reviews instead | 1598 lines; two parts, one gate. **Rev 2** applies the first adversarial review (five reviewers; 7 HIGH + 14 MEDIUM + 18 LOW, all fixed): `resolve_model` reads `config.llm.model` verbatim and the default endpoint is per-document section selection (`21 §7.7`) layer-by-layer (not "nested wins"); colliding endpoint/model entries replace **wholesale** (52-F18 guards a shadowed global literal credential); import notes are collected in the mapping pass (note count == skip count; per-entry skip, never whole-import abort); `PersonaRow` deleted in favor of `std::optional<PersonaConfig>`; `headers` is global-layer-only + redacted (52-F22); `list()`/`reserved_ids()` pinned; OQ-1/8/10/12 resolved; failure modes renumbered contiguous **F1–F23** (Part A F1–F10 + F18–F23, Part B F11–F17; old F18 merged into F16); §1.4/§5.1/§5.2/§5.7 state plainly that prompts are the weak leg (persona only) and record 52-OQ-13. **Part A** — named endpoints (`llm.endpoints.<n>`) and named models (`llm.models.<n>`), `llm.active_model`, a total `resolve_model`, the strict-loader `reject_unknown_named_map` extension, and the all-providers/all-profiles localcode import (incl. the `bedrock` skip and the per-model model-profile correction). **Part B** — the shipped dsh-aligned agent-preset roster (`minimal`/`standard` shipped, `ptc`/`cordis` reserved-and-fail-loud) on top of verified spec 42, the shipped-presets install root, the persona `complete`/`include_runtime_context` row extension, `permissions.presets` + session-pinned permission binding, and the in/out capability list. Confirms `00` §55's "Cordis-compatible configuration" omission stands; realizes `00` §38. 13 open questions (4 resolved). |
| 53 | `53-eager-daemon-and-model-switching-errata.md` | yes | **verified (Rev 2)** | Five-reviewer adversarial gate (scope, spawn, switch, iface, completeness): Rev 1 → **8 HIGH / 21 MEDIUM / 22 LOW** → Rev 2 → re-check: all findings FIXED. The re-check itself caught **8 fresh inconsistencies introduced by the fixes** (a self-referential citation to `01 §3`, a `display_model` call with no viable overload, an endpoint guard comparing provider id instead of endpoint identity, a provider-side profile overclaim, and four stale anchors). 0 open HIGH/MEDIUM. Supersedes 49's lazy-spawn. | Four requirements from the user's verbatim request: the daemon is created **on start** (not lazily), the bottom status line shows the **model name** from the first frame, `/model` **lists** spec 52's named models and lets the user **pick**, and the daemon accepts a **mid-flight model change**. **Supersedes 49-D1/49-D2** (the lazy-spawn decision and the empty state as the normal bare-`ymh` start) and **re-supersedes 16-D2's bare-cwd lazy policy** (restoring 22 §11.4/22-A7's eager initial spawn; 22-A7's S3 scope extension retained). Half A: 53-D1 eager cwd workspace/daemon creation at startup, 53-D2 the shipped attach auto-create (46-I13/23-D58/50-D2.1) gives the first session, 53-D3 the status-line model segment + a 25-D1 `active == nullptr` amendment. Half B (amends 08 §5.2/L11 `SessionHeader.model` immutability): 53-D4 a `UiMode::ModelPicker` over `config.llm.models` (keys reuse 45-D2/45-D5/22-A2), 53-D5 the new Interactive-only `session.set_model` RPC (mirrors 25-D5's `session.set_mode`; queues behind an open turn), 53-D6 the durable `SessionModelChanged` event (`session/model`; last-wins; `sessions.model` materialized in the same transaction), 53-D7 a `ModelSelectionController` + `ModelCatalog` + `AgentLoop` seam (the switch carries the entry's parameters/profile/provider, not just the id; cross-endpoint targets rejected with the new `EndpointNotRouted` AppCode `-32021` because the daemon registers one route), 53-D8 errors. Invariants 53-I1–53-I13; failure modes 53-F1–53-F13; amendment register 53-A1–53-A13; dsh mapping (per-session directory, provider-grouped, "a running step keeps the model it started with"); 9 open questions (initial session vs. config-model-only display, per-session vs. workspace default, multi-endpoint routing, type-to-filter, display-name [resolved by 53-D3.1], endpoint segment, Automation profile, reasoning effort, subagent model inheritance). Rev 2: five-reviewer finding set applied — eager-daemon ownership corrected (it IS orphaned; last-exit modal), 08 §5.2 (L11) added as an amended verified spec, the model switch carries parameters/profile/provider (not just the id), `session.create` resolves the name to a wire id, and the test plan completed. Verification status: DRAFT (Rev 2) — not yet reviewed |
| 54 | `54-multi-endpoint-routing-errata.md` | yes | **verified (Rev 6)** | Five-reviewer adversarial gate, **six rounds**: Rev 1 → **11 HIGH / 13 MEDIUM / 16 LOW** → Rev 6 → bounded re-check: all four Rev 6 changes CONSISTENT, 0 open HIGH/MEDIUM. Notable catches: a silent wrong-endpoint credential fallback, a RouteResolver that could not carry its own inputs, an AdapterHandle owner whose destructor unregisters the route, and a byte-identity claim that was overbroad because `llm.profile` is independent of named endpoints | 1289 lines; fixes the reported `/model` `EndpointNotRouted` defect on a cross-endpoint pick. Resolves 53-OQ-3: the daemon routes per request to the endpoint named by the session's effective model selection. Chosen routing model: **lazy, cached, per-endpoint provider construction** (option b) — provider construction is cheap and stateless, but eager construction of every `llm.endpoints` entry would turn a keyless/broken unused endpoint into a daemon-start failure (contra 52-F23). New `LlmCallConfig::endpoint` (endpoint NAME; `""` = anonymous default) and `profile_id` (`ModelProfile::id`; `""` = inert), both omitted when empty to preserve 52-I5 byte-identity; typed, profile-aware route keys (`RouteKey{Endpoint,name,profile_id}` for named endpoints, `RouteKey{Provider,id,""}` for the default), with `endpoint_handles_` keyed by the same `(endpoint, profile_id)` pair as the cache; `resolve_adapter` selects per request with a provider fallback **only when the endpoint is empty** (a named endpoint never falls back) and a loud `NoProviderRouteError` on a miss. The 53-I9 endpoint guard is superseded: it now tests membership of the target endpoint **name** in the daemon's routable set (no `base_url` comparison, `ProviderId` not consulted), **selecting** a route instead of rejecting a differing endpoint; it rejects only a target outside the daemon's routable set (unreachable for a valid config per 52-I2), and `EndpointNotRouted` is retained defensively. 53 §10.1's provider-side profile limitation is **narrowed, not removed**: adapter profile is bound per `(endpoint, profile_id)` at construction (a switch changing the endpoint or the profile id re-binds it; keeping both does not); the residual per-request adapter profile is 54-OQ-1 (supersedes 53-OQ-10). Also pins: compaction routes to the session's endpoint (54-D7), `SessionHeader.model_name` folds for unambiguous resume (54-D8), per-endpoint key/header containment (54-I10), and exactly one provider/route per `(endpoint, profile_id)`, serialized under the cache mutex (54-I11). 54-I1–54-I13, 54-F1–54-F12, 54-U1–54-U22, 54-OQ-1–54-OQ-9. Rev 2 applies the five-reviewer finding set (8 HIGH / 11 MEDIUM / 15 LOW): the failure-path silent wrong-endpoint fallback is gated, `RouteResolver` carries model identity (`endpoint_name, model_id, profile_id`), the route cache is keyed by `(endpoint, profile_id)` with the resolver retaining each `AdapterHandle`, `set_route_resolver` is one-shot (54-I13), the durable `model_name` is carried through `SessionOptions`/`SessionStarted`/fork and persisted in the `sessions` table, and the test plan gains 54-U18–54-U21. Rev 3 applies the re-check finding set (1 HIGH / 3 MEDIUM / 5 LOW): the Endpoint-kind route key becomes profile-aware (`RouteKey{Endpoint,name,profile_id}`) and `register_endpoint_route`/`endpoint_handles_` carry the same pair, so the per-`(endpoint,profile_id)` rebind the spec claims is actually reachable (previously a second profile hit the first profile's name-only route); the construction-race clauses are reworded to the serialized-under-`endpoint_cache_mutex_` mechanism (no losing provider); `model_id` is dropped as an inert resolver parameter and `Registration.routes` becomes `std::vector<RouteKey>`; test 54-U22 covers the 54-I13 one-shot; §8.4 gains `LlmCallConfig::endpoint`. **Rev 4** applies two residual re-check findings (1 MEDIUM / 1 LOW) exposed by the profile-aware route key: the empty/unknown `profile_id` binding is pinned (the resolver substitutes a function-local static inert `ModelProfile{}` when `find_model_profile` returns `nullptr` — `total` means does-not-throw, not non-null — so the supported `profile_id == ""` case has a defined non-null binding; option (a)), and the compactor now sets `config.profile_id` from the **same** durable-selection source as `config.endpoint`, so the summarizer builds/reuses the session's `(endpoint, profile_id)` provider instead of the inert `(endpoint, "")` one (previously masked by the endpoint-only key). **Rev 5** applies four residuals from two independent re-checks (1 MEDIUM / 3 LOW): 54-U1's false provider-config-identity clause is reduced to the inert-profile treatment, the byte-identity claim is qualified to configs with no named endpoints **and an empty default profile** (54-D2/54-I6/54-A14), the §4 compactor helpers are renamed `durable_endpoint_name`/`durable_profile_id` with a must-not-call-`effective()` note, and 54-I2 is scoped to named-endpoint pairs. **Rev 6** applies the final fix pass (2 MEDIUM / 2 LOW): the compactor now pairs `config.profile_id` with the requested summarizer model (54-D7/§4/54-OQ-7) rather than the session's, the third `ModelSelection` aggregate-init site (`agent_loop.cpp:539`) gains a pinned `AgentConfig::endpoint` source (54-D2/54-A9), 54-F8/54-U12 no longer claim the inert pair is never used when the requested model has no profile, and the within-endpoint profile-rebind clauses (54-I9/54-D6/54-U18) are qualified to named/Endpoint-kind routes. Verification status: DRAFT (Rev 6) — not yet reviewed |
| 55 | `55-multi-agent-delegation-errata.md` | yes | **verified (Rev 6)** | Five-reviewer adversarial gate, **six rounds**: Rev 1 → **13 HIGH / 25 MEDIUM / 22 LOW** → Rev 6 → bounded re-check: 4 of 5 reviewers reported, 3 all-consistent, every raised item fixed; 0 open HIGH/MEDIUM. Notable: the job machinery the settlement design assumed was unwired; the child's durable route was unrebuildable; a TOCTOU fan-out cap; three pinned-symbol access defects; a replay rule that would have spammed foreground delegations; and an idempotency ordinal off by one (four reviewers). — late seventh-reviewer re-check applied (1 MEDIUM / 4 LOW: the `notice_expected` predicate was too narrow — a `send_message`-opened epoch was not background-registered, so its fan-in would be skipped by replay; §4 snippet compile fix; replay wording; finding-id legend), not yet re-verified | — (design-first gate pending; Rev 6 needs re-check) | 1956 lines; the delegated-subagent capability, modelled faithfully on dsh `@deepseek-ai/dsh-tool-subagent` + `-control`: **both** modes (`one-shot`, `continuable`) and the full per-child route surface. Pins the tool family (`subagent`, `subagent_continuable`, `send_message`, `interrupt_agent`, `list_agents`, `list_subagent_models`), the `run_in_background` truth table, the durable-child/restart semantics (a resident session in the same daemon; `childId` = `SessionId`), the **settlement notice** = a durable `SubagentFanIn` + a `kind==subagent` job notice with a **replay rule** (**at-least-once**, not exactly-once), an activation as a **residency epoch** with drain semantics, the route-selection precedence (**provider defaults (dsh-only, not mirrored) → configured → call → effort merge → route preflight → parent's effective `ModelSelection`**; model/effort validated against a **required** catalog; `(endpoint, profile)` persisted on `SessionHeader`), `maxDepth` reusing `presets.max_depth` plus a new **`ResourceCaps::max_live_subagents`** fan-out cap enforced at `AgentRegistry::createChild` admission with an **atomic reservation**, per-child `persona`/`toolFilter` via `apply_child_composition` (first production call site), the errors-never-partial-success contract, and the **mandatory `SubagentRunner` settlement-race fix** (`src/agent/subagent.cpp:49`) via a new `Agent::onSettled`. Invariants 55-I1–55-I36; failure modes 55-F1–55-F22; 13 amendments; 40 tests + the 5 existing doubles that must change; 12 open questions. **Rev 2** applies the first adversarial gate (five reviewers; **13 HIGH + 18 MEDIUM + 14 LOW**, all fixed): the job subsystem is **entirely unwired** (`44:87`) so it is built here, not reused; settlement is durable-with-replay; activation is an epoch; the durable route is `(endpoint, profile)`; the preflight validates route only; `ModelSelectionController`+`SessionActivator` are injected; admission is an atomic reservation; §4 shows `AgentRegistry` members; F19/F20 have tests; the five `FakeAgent` doubles are named; §7 parity and §8 register reconciled. **Rev 3** applies the second re-check (four reviewers) + the mandatory symbol/access-level sweep (**2 HIGH + 8 MEDIUM + 10 LOW**, all fixed): the private `resolve_adapter` preflight call is replaced by a public `LlmRuntime::preflight_route` (55-A10); `onSettled` fires at the terminal event with a separate drain check; `SessionActivator`/`RouteCatalog`/`SubagentRunner` are pinned in §4; `settle` keeps a continuable child resident (one-shot disposes); the durable route is the `(endpoint, profile_id)` pair; model/effort validation is owned by `AgentRegistry::createChild`; and F13/F17/I1/I25/I26/I28/I30/I35 gain tests (U34–U40). **Unwired-machinery facts verified:** `SubagentRunner`/`apply_child_composition`/`check_delegation_depth`/`JobRegistry`/`JobWakeupPolicy`/job tools have zero production call sites (tests only). **Rev 5** applies the sixth-reviewer re-check (three converging reviewers; **2 HIGH + 3 MEDIUM + 4 LOW**, all fixed): the settlement-correlation ordinal is pinned as `existing_count + 1` so the fast-path `notice_plugin` marker and replay's derived ordinal agree (55-R5-H1); the dsh notice text gains a pinned `JobSnapshot`/`JobOutcome::notice_text` seam (55-R5-H2); `finish_reason` is registered as **55-A13** with its omit-when-default codec rule (55-R5-M2); `JobRegistry::start`/`settle` are pinned to copy the notice fields into the snapshot (55-R5-M3); and §1.4 discloses the Rev 4 durable additions (A10/A11/A12; 55-R5-M4). **Rev 6** applies the late seventh-reviewer re-check (1 MEDIUM + 4 LOW): the `notice_expected` predicate is redefined as "the epoch was not opened by a foreground-blocking activation", so every non-foreground epoch — a background start **or** a `send_message`-opened epoch — is background-registered and gets a notice, reconciled across D4/D13/§4/A11/I7/I31/I32/F19/U16/U18/U28/U29 (55-R6-M1); the §4 `JobStart`/`JobSnapshot`/`JobOutcome` notice-field snippet is shown inside each struct so it compiles (55-R6-L1); D4's replay rule is reworded to at-least-once per replay pass (55-R6-L2); §12 gains a finding-id legend mapping `55-Hn` to the Rev 2 `H1`–`H13` (55-R6-L3); and the F1/F4/F6 test-row and §11 spec-`28` citations are confirmed (55-R6-L4) |

| 56 | `56-dsh-fidelity-gap-closure-errata.md` | yes | **verified (Rev 2)** | Five-reviewer adversarial gate: Rev 1 → **5 HIGH / 11 MEDIUM / 13 LOW** → Rev 2 → re-check: all findings FIXED, 0 open HIGH/MEDIUM. Notable catches: a verbatim reorder that would have emptied every default prompt (an early return **and** the block's tail return), a pinned wiring reading a moved-from config, an unenforceable process convention, and a missing amendment of the frozen `part2` `AssembleContext` sketch. | Closes the `tool:<toolName>` system-prompt guidance gap in spec 55: pins the verbatim dsh delegation guidance, emits it per continuable delegation instance at the reserved `TOOL_SUBAGENT` order (2800), and gates it on a new fail-closed `AssembleContext::visible_tools` seam so a tool restriction removes both the schema and the guidance (the whole point). Also disposes of each recorded 55 §7 non-mirror with a justification instead of a record (`modelSelectionSettings` = deliberate scope decision; `provider.agentRouteDefaults` = unbuilt-feature deferral; `maxDepth: 'provider-managed'` = unbuilt-feature deferral, additive row; `fork` = deliberate scope decision; `acp` = justified by `00` §55's deliberate plugin-ecosystem omission) and pins the "a non-mirror must carry a reason" convention (with a concrete anchor + reviewer gate) in AGENTS.md. Invariants 56-I1–56-I14; failure modes 56-F1–56-F11; tests 56-U1–56-U15; open questions 56-OQ-1–56-OQ-5. Amends 36 §2.1/§2.6 and `26-dsh-alignment-part2.md` §4.3.3; supersedes specific 55 §7 rows. **Rev 2** applies a five-reviewer finding set (5 HIGH / 11 MEDIUM / 10 LOW, all fixed): the `assemble()` reorder no longer moves the empty-`tool_order_` early return (H1); the wiring captures `tool_name` before the by-value move (H2) and includes `prompt/order.hpp` (H3); 56-D6 gains a concrete-anchor + reviewer-gate obligation (H4); part2 §4.3.3 is recorded as amended (H5); `tool_order` is corrected to reorder-never-narrow (M1); and the remaining citation/consistency fixes. |
| 57 | `57-switcher-sessions-popup-errata.md` | yes | **verified (Rev 1)** | Oracle gate, 5 rounds: round-5 final pass **PASS** (0 open HIGH/MEDIUM; residual LOWs non-blocking and recorded) | Additive errata to `22-switcher-sessions-errata.md` (verified) and `51-reasoning-fold-prompt-emphasis-and-tables-errata.md` (verified) for the Ctrl-S (`Live`) / `/sessions` (`History`) overlays: drops the frozen `captured Ns ago` footer segment and its `capturedAtMs` carrier, adds the persistent `Ctrl+D delete` hint, renders the two-press delete confirmation inline on the target's own row as a red badge (never an appended row), pins popup Ctrl+Q ownership so it never exits, and orders the effective-root workspace first (then `lastUsedAt` desc). Invariants 57-I1–57-I20, failure modes 57-F1–57-F14, decisions 57-D1–57-D7. Supersedes 22-D1's workspace-ordering half, SW17, and SW19's `captured` segment; the 51 arm/confirm semantics are retained verbatim. 1461 lines. |

## Open top-level items (`HANDOFF.md` §5)

| # | Item | Status | Resolution |
|---|---|---|---|
| 1 | Naming: `txtcoder` vs legacy `ymh` | **resolved** | `ymh` is final (binary, state, workspace dir, source tree, namespace); `txtcoder` retired |
| 2 | Registry bootstrap discovery (§9.10) | **resolved** | registry + `flock` primary; exact-match process scan is best-effort fallback only |
| 3 | `SessionHeader` final field list | **resolved** | §9.2/§9.10: no boot nonce in header; `ordinal`/`archived` stay in registry junction |
| 4 | Remote/SSH TCP transport | **out of scope** | DECISION (user, 2026-09-17): not deferred, not planned, will not be built; JSON-RPC stays transport-agnostic |
| 5 | Deferred dsh items | accepted | no offline cache; host events merged into mux; no hot reload |

## Operating requirements (2026-09-14)

- **Autonomous development.** Design and coding proceed with minimal user
  intervention. Only non-obvious design or user-interface questions are routed
  to the user.
- **Live end-to-end testing.** A real LLM API is used to drive the TUI in a PTY
  as a human would (start session, type prompts, observe rendered output).
- **Coverage target.** Deterministic Fake-LLM layer for unit/integration/golden/
  replay; live layer is opt-in (requires an API key). Top-level
  (supervisor/manager) components are targeted for 100% automated coverage.

## Review history

| Date | Artifact | Reviewer | Result |
|---|---|---|---|
| 2026-09-14 | `00-architecture.md` | Oracle pass 1 | 18 fixes applied (from handoff) |
| 2026-09-14 | `00-architecture.md` | Oracle pass 2 | GATE FAIL — 9 MEDIUM, 7 LOW |
| 2026-09-14 | `00-architecture.md` | Oracle pass 3 | **GATE PASS** — all 16 resolved; 5 new LOW cleaned up |
| 2026-09-14 | `01-session.md` | Oracle component pass 1 | GATE FAIL — 4 MEDIUM, 3 LOW, S11–S14 undefined |
| 2026-09-14 | `01-session.md` | Oracle component pass 2 | **GATE PASS** — 7 resolved; 2 new LOW cleaned up |
| 2026-09-14 | `02-persistence.md` | Oracle component pass 1 | GATE FAIL — 4 MEDIUM, 3 LOW |
| 2026-09-14 | `02-persistence.md` | Oracle component pass 2 | **GATE PASS** — 7 resolved; 2 LOW cleaned up |
| 2026-09-14 | `03-workspace-registry.md` | Oracle component pass 1 | GATE FAIL — 2 MEDIUM, 3 LOW |
| 2026-09-14 | `03-workspace-registry.md` | Oracle component pass 2 | **GATE PASS** — 5 resolved; 1 LOW cleaned up |
| 2026-09-14 | `04-workspace-host-daemon.md` | Oracle component pass 1 | GATE FAIL — 2 MEDIUM, 4 LOW |
| 2026-09-14 | `04-workspace-host-daemon.md` | Oracle component pass 2 | **GATE PASS** — 6 resolved; 1 LOW cleaned up; focus-independence confirmed |
| 2026-09-14 | `05-transport.md` | Oracle component pass 1 | GATE FAIL — 1 HIGH, 5 MEDIUM, 7 LOW |
| 2026-09-14 | `05-transport.md` | Oracle component pass 2 | **GATE PASS** — 13 resolved; 1 LOW cleaned up |
| 2026-09-14 | `08-llm-provider.md` | Oracle component pass 1 | GATE FAIL — 2 MEDIUM, 6 LOW |
| 2026-09-14 | `08-llm-provider.md` | Oracle component pass 2 | **GATE PASS** — 8 resolved |
| 2026-09-14 | `01-session.md` | Oracle errata check | **GATE PASS** — `turn/fail` additive errata; no regressions |
| 2026-09-14 | `06-agent-loop.md` | Oracle component pass 1 | GATE FAIL — 2 HIGH, 7 MEDIUM, 7 LOW |
| 2026-09-14 | `06-agent-loop.md` | Oracle component pass 2 | **GATE PASS** — 16 resolved; cross-doc `TurnFailed.code` fixed via 08 §4.5 errata |
| 2026-09-14 | `07-tools-execution.md` | Oracle component pass 1 | GATE FAIL — 2 HIGH, 10 MEDIUM, 4 LOW |
| 2026-09-14 | `07-tools-execution.md` | Oracle component pass 2 | **GATE PASS** — 16 resolved; dangling OQ labels cleaned up |
| 2026-09-14 | `09-permissions.md` | Oracle component pass 1 | GATE FAIL — 4 MEDIUM, 8 LOW |
| 2026-09-14 | `09-permissions.md` | Oracle component pass 2 | **GATE PASS** — 12 resolved; 3 LOW cleaned up |
| 2026-09-14 | `10-supervisor-tui.md` | Oracle component pass 1 | GATE FAIL — 1 MEDIUM, 7 LOW |
| 2026-09-14 | `10-supervisor-tui.md` | Oracle component pass 2 | **GATE PASS** — 8 resolved; focus-independent flash confirmed |
| 2026-09-15 | `11-m2-errata.md` | Oracle G0 pass 1 | GATE FAIL — 6 HIGH, 8 MEDIUM, 7 LOW |
| 2026-09-15 | `11-m2-errata.md` | Oracle G0 pass 2 | GATE PASS — 21 resolved + R1–R3; interfaces frozen |
| 2026-09-15 | `11-m2-errata.md` §7.2 | track E finding (post-gate, additive) | `Clock&` inert → injectable `ClockReader` (default `Clock::now`); D19.1 + E21 + §16 updated; no other interface semantics changed |
| 2026-09-15 | `12-m1-drift-errata.md` | track F (self) | written — M1 spec/code reconciliation register DR1–DR6; no new semantics; defers to 11; pending review |
| 2026-09-16 | `16-daemon-ownership.md` | round 1 (rev 0) | GATE FAIL — critic 4 HIGH/10 MEDIUM/5 LOW; Oracle 2 HIGH/11 MEDIUM/8 LOW (incl. safety-property breach: `FRESH_OWNER` clock mismatch) |
| 2026-09-16 | `16-daemon-ownership.md` | round 2 (rev 1a) | GATE FAIL — closure 40/41 + 21/21; **NEW** 1 HIGH (cached-count cannot exclude the requester), 5 MEDIUM, 13 LOW |
| 2026-09-16 | `16-daemon-ownership.md` | round 3 (rev 2) | GATE FAIL — closure 7/7 + 14/15; **NEW** 1 HIGH (fail-open `require_owner` default), 2 MEDIUM, 7 LOW |
| 2026-09-16 | `16-daemon-ownership.md` | round 4 (rev 3) | GATE FAIL — closure 8/9; **NEW** 1 HIGH (guard omitted `watchdog_disabled`), 2 MEDIUM, 6 LOW; adjudicated Oracle `G3` over the critic (seam retyping was overstated) |
| 2026-09-16 | `16-daemon-ownership.md` | round 5 (rev 4) | **GATE PASS** — critic 0 HIGH/0 MEDIUM (closure 3/3); Oracle 0 HIGH/0 MEDIUM (closure 9/9); 5 LOW remained |
| 2026-09-16 | `16-daemon-ownership.md` | final polish (rev 5) + targeted Oracle confirmation | **PASS** — 5/5 LOW fixed, 0 regressions; O15 bounds unchanged (23 s/38 s); escalation budget 12 s → 34 s confirmed exit-bound, not request-bound |
| 2026-09-16 | `17-ui-transcript-errata.md` | round 1 | GATE FAIL — 1 HIGH (cross-thread `UiModel` mutation for the session title), 7 MEDIUM, 6 LOW |
| 2026-09-16 | `17-ui-transcript-errata.md` | round 2 | GATE FAIL — closure 13/14; F-02 residual (text-then-reasoning ordering unpinned while claiming "always `[…, Reasoning, Assistant, …]`"), 3 LOW |
| 2026-09-16 | `17-ui-transcript-errata.md` | round 3 | **GATE PASS** — 0 HIGH/0 MEDIUM/0 LOW; closure 4/4; no regressions. Implemented in `71dda4f16` |
| 2026-09-16 | `16-daemon-ownership.md` | implementation (8 waves) | `036e4e4e1` (foundation) → `3a38334a0` (registry+migration) → `9db080868` (watchdog+guard+typed seam) → `15b757f75` (admission+ownership view+broadcast) → `eb3774cb3` (identity+presence+CLI) → `cbaca8437` (exit prompt+session visibility+marks) → `5bea22e3d` (workspace stop) → `0753fb583` (test rewrites+two-supervisor+crash injection) → `71850ae0d` (two-spawn handoff fix) → `065e221d9` (last-exit stale-snapshot fix). 792 tests, 0 orphans; verified live with two supervisors against real DeepSeek |
| 2026-09-16 | `16-daemon-ownership.md` | post-implementation bugs found by live verification | (1) the two-spawn race burned the full readiness budget retrying the loser's boot nonce → abort under load (`71850ae0d`); (2) exiting the LAST supervisor could silently exit without prompting and **leak the daemon**, because `is_orphaning_view` trusted the daemon's 2 s-cached `other_fresh_owners` snapshot and saw a just-exited peer as a ghost owner (`065e221d9`). Both hermetic-suite gaps are now covered by tests |
| 2026-09-20 | `45-ui-interaction-errata.md` | round 1 (rev 1) | GATE FAIL — adversarial critic 2 HIGH / 10 MEDIUM / 8 LOW (a false `session.show` current-state claim in D7.2; D4.3 silently contradicting `22 §4.3`/`§4.4`; the real `activeSessionId` setter `activate_session` unamended so 45-I10 did not hold) |
| 2026-09-20 | `45-ui-interaction-errata.md` | round 2 (rev 2) | GATE FAIL — closure 11/12; 1 MEDIUM (the History source rendered a false `(no stored sessions)` when the workspace's only session was the hidden focused one) |
| 2026-09-20 | `45-ui-interaction-errata.md` | round 3 (rev 3) | **GATE PASS** — 0 HIGH / 0 MEDIUM; closure 12/12; no new inconsistencies; `file:line` citations re-verified against the shipped tree |
| 2026-09-20 | `45-ui-interaction-errata.md` | round 4 (rev 4, Oracle design pass) | **DO NOT APPROVE — 4 HIGH / 6 MEDIUM** (design flaws the citation critic missed: `agent.list` omitting the daemon-owned 42-I3 predicate; hiding the focused session could silently empty the switcher and hide live/focusable workspaces; Tab cycling on arbitrary `/zzz` drafts and `preferred_agent_` shown as active; protocol-version neutrality + no `MethodNotFound` fallback) |
| 2026-09-20 | `45-ui-interaction-errata.md` | round 5 (rev 5) | GATE FAIL — closure 10/10 Oracle HIGH+MEDIUM; critic 3 MEDIUM / 3 LOW remained (`skipped` shape mismatch; false "every node is focusable"; false `(no live sessions)` placeholder) |
| 2026-09-20 | `45-ui-interaction-errata.md` | round 6 (rev 6) | GATE FAIL — closure 6/6; 1 MEDIUM / 2 LOW remained (a shared `mcp_server_json` builder placed in `ymh_agent` could not compile — that layer cannot see `McpServerStatus`; `StatusModel::agent` event-projection claim; incomplete `activeSessionId_` migration list) |
| 2026-09-20 | `45-ui-interaction-errata.md` | round 7 (rev 6 final) | **GATE PASS** — 0 HIGH / 0 MEDIUM; 1 LOW (an inaccurate rationale for declining a shared builder: `ymh_agent` does transitively reach `ymh_mcp` via `ymh::config`; the two-serializer design is valid and stands) |

